name: signup
method: POST
url: $BASE_URL/signup
body:
  content: |-
    {
      "username": "$BASE_USERNAME",
      "password": "$BASE_PASSWORD"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
